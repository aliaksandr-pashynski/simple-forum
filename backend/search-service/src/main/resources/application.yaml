spring:
  application:
    name: search-service

  cloud:
    consul:
      config:
        format: YAML
        data-key: data
        fail-fast: true
      discovery:
        healthCheckPath: ${server.servletPath}/actuator/health
        healthCheckInterval: 15s

server:
  port: 8081
  servlet:
    context-path: /search-service